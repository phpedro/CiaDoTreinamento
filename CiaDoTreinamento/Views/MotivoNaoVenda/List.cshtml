@model List<CODE.MotivoNaoVenda>

<section id="main-content">
	<section class="content">
		<div class="panel">
			<header class="panel-heading">
				<h4>MOTIVOS NÃO VENDA</h4>
				<div class="top-nav">
					<button id="btnNovo" class="btn btn-primary" onclick="jsNovo();"><span aria-hidden="true" class="zmdi zmdi-file-plus"></span> &nbsp; NOVO</button>
				</div>
			</header>
			<div class="panel-body">
				<div class="table-responsive">
					<table id="data-table" class="table table-bordered table-striped">
						<thead class="thead-default">
							<tr>
								<th class="center">AÇÕES</th>
								<th class="center">CÓDIGO</th>
								<th class="center">DESCRIÇÃO</th>
							</tr>
						</thead>
						<tbody>
							@foreach (CODE.MotivoNaoVenda motivo in Model)
							{
								<tr>
									<td class="center">
										<a href="#" title="Editar" onclick="jsEditar(@Html.Raw(motivo.Codigo))"><span aria-hidden="true" class="zmdi zmdi-border-color"></span></a>
										&nbsp;
										<a href="#" title="Excluir" onclick="jsExcluir(@Html.Raw(motivo.Codigo))"><span aria-hidden="true" class="zmdi zmdi-delete"></span></a>
									</td>
									<td class="center">
										@motivo.Codigo
									</td>
									<td class="center">
										@motivo.Descricao
									</td>
								</tr>
							}
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</section>
</section>

<script type="text/javascript">

	function jsEditar(codigoMotivo) {
		window.location.assign("@Url.Action("Edit", "MotivoNaoVenda")?codigoMotivo=" + codigoMotivo);
	}

	function jsExcluir(codigoMotivo) {
		window.location.assign("@Url.Action("Delete", "MotivoNaoVenda")?codigoMotivo=" + codigoMotivo);
	}

	function jsNovo() {
		window.location.assign("@Url.Action("Edit", "MotivoNaoVenda")");
	}

</script>